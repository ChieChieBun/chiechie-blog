
<h1> Journals for <%= @user.username %>  </h1>
<%= link_to "New Journal", new_user_journal_path(@user) %>
<ul>
<% @journals.each do |journal| %>
      <li><%= link_to journal.title , user_journal_path(@user, journal) %></a></li>
    <% end %>
</ul>
